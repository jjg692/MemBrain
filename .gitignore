__pycache__/
*.pyc
.venv/
env/
chromadb/
models/
uploads/
static/avatars/agents/*.png
.env.local
*.log
.DS_Store
.env

# Live2D 模型数据（体积大、版权模型）：只忽略根目录 live2d/ 下的模型二进制，
# 不忽略 static/live2d/runtime 下的运行时 JS（体积小、可入库便于离线）
/live2d/
# 运行时数据（用户昵称/提醒等本地持久化，不入库）
user_profiles.json
reminders.json
perception.json
relation_memory.json
relation_memory_*.json

# 测试：纳入源码（test_*.py / conftest.py / README.md），排除生成物
/test/__pycache__/
/test/test_results.txt

# 本地回滚备份区域（不进 git）
/backup/

# ===================== 星露谷 MCP 扩展（stardew/） =====================
# 整个 stardew/ 目录从 git 跟踪中移除（本地保留），不再纳入版本库。
# 含第三方 StardewValley-MCP server 与项目自制接入代码。
/stardew/

# ===================== 星露谷 MCP 扩展（stardew/） =====================
# 整个 stardew/ 目录从 git 跟踪中移除（本地保留），不再纳入版本库。
/stardew/
# 依赖 stardew/ 的星露谷测试文件也一并移出（本地保留）
test/fake_stardew_mcp.py
test/stardew_test.py
test/stardew_e2e_test.py
test/ollama/test_stardew_runtime.py

# ===================== 本地工具（tools/） =====================
/tools/

# ===================== 红警 MO 接入插件（redalertmo/） =====================
redalertmo/
