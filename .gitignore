__pycache__/
*.pyc
.venv/
env/
chromadb/
models/
uploads/
static/avatars/agents/*.png
.env.local
*.log
.DS_Store
.env

# Live2D 模型数据（体积大、版权模型）：只忽略根目录 live2d/ 下的模型二进制，
# 不忽略 static/live2d/runtime 下的运行时 JS（体积小、可入库便于离线）
/live2d/
# 运行时数据（用户昵称/提醒等本地持久化，不入库）
user_profiles.json
reminders.json
perception.json
relation_memory.json
relation_memory_*.json

# 测试：纳入源码（test_*.py / conftest.py / README.md），排除生成物
/test/__pycache__/
/test/test_results.txt

# 本地回滚备份区域（不进 git）
/backup/

# ===================== 本地工具（tools/） =====================
/tools/

# ===================== 每角色 TTS 配置（本地机器专属路径） =====================
# 每角色 TTS 服务的权重/参照音频/执行文件路径是本机专属，不入库；生成的 yaml 同理
config/tts_roles.json
config/tts_roles_configs/

# ===================== 前端优化回滚备份（本地保留，不入库） =====================
templates/*.bak

# ===================== 群聊历史落盘（运行期数据，不入库） =====================
rooms_history.json
rooms_history.json.tmp

