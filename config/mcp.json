{
  "_comment": "MCP 客户端配置。每个 server: {command, args, env}。暴露的工具会自动注册到 LLM。由 core/config.py 的 STARDEW_MCP_ENABLED 总开关控制是否加载。路径可用 {PROJECT_ROOT} 占位符锚定到项目根。现代服务使用 luy-0/StardewValley-MCP（uv + Python MCP 服务器 + SMAPI Mod，TCP/protobuf/共享秘钥握手）。共享秘钥(≥32字节 base64) 由 Mod 首次启动时生成于 Mods/StardewValleyMCP/config.json，请将它填入下面 SHARED_SECRET。只读(默认) 用 ["serve"]；允许操作游戏加 --allow-write。",
  "servers": [
    {
      "name": "stardew",
      "command": "stardew-valley-mcp",
      "args": ["serve", "--allow-write"],
      "env": {
        "STARDEW_VALLEY_MCP_HOST": "127.0.0.1",
        "STARDEW_VALLEY_MCP_PORT": "24642",
        "STARDEW_VALLEY_MCP_SHARED_SECRET": "<填入 Mod config.json 中的 SharedSecretBase64>"
      }
    }
  ]
}
